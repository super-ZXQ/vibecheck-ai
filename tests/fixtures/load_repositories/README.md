# Synthetic load fixture repository

This directory holds **synthetic** mini-repositories used for local load and
fault experiments. They are intentionally tiny and contain no real secrets
and no third-party proprietary code.

Create additional fixtures as plain directories or zip files when running
manual upload load tests. Do not point load tools at public GitHub at rate.
